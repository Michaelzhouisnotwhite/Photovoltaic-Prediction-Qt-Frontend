{
    "configurations": [
    
        {
            "name": "Launch qt main",
            "type": "python",
            "request": "launch",
            "env": {
                "PYTHONPATH": "${workspaceFolder};${workspaceFolder}/ui;"
            },
            "program": "${workspaceFolder}/main.py",
            "cwd": "${workspaceFolder}"
        }
    ]
}